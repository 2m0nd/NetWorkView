<UserControl x:Class="NetworkView.AdapterSelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <DataGrid MinWidth="300" IsReadOnly="True" Name="gridAdapters" AutoGenerateColumns="False" MouseDoubleClick="gridAdapters_MouseDoubleClick_1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Adapter name" Binding="{Binding Path=Name}"/>
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
</UserControl>
